"use client";

import { useEffect } from "react";
import { usePathname } from "next/navigation";

export function ScrollToTop() {
//   const pathname = usePathname();

//   useEffect(() => {
//     const scrollPositionBefore = window.scrollY;
//     console.log("Posición del scroll al principio:", scrollPositionBefore);
    
//     console.log("pathname", pathname);
//     window.scrollTo(0, 0);
    
//     const scrollPositionAfter = window.scrollY;
//     console.log("Posición del scroll al final:", scrollPositionAfter);
//   }, [pathname]);

  return null;
}